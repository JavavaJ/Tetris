package tests;

public class Figure {

	// create an enum holding position
	enum Position {
		HORIZONTAL,
		VERTICAL
	}
    
    // set initial position to vertical 
    Position position = Position.VERTICAL;
}